# Migrations

Server upgrades run `migrations/run <old-version> <new-version>` before `setup/install`.

Use this directory for one-off host or repo transitions that should happen only
when crossing specific versions.
